/** Settings page — categories, preferences. Built in Phase 2. */
export default function Settings() {
  return (
    <div>
      <p className="font-poppins text-body-md text-mech-ink-50">Settings — Phase 2</p>
    </div>
  )
}
